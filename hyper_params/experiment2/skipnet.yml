project: Thesis
name: Skipnet baseline

parameters:
    model name: "SkipNet"
    model class: "SkipNetModel"
    encoder type: "gated"

    use edges: False

    batch size: 8
    lr: 0.0001
    monitor: "val_l1_depth_loss"
    epochs: 7
